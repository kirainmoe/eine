import { EineRouteHandlerType } from "../types";
declare function magic({ res, server }: EineRouteHandlerType): void;
declare const _default: {
    handler: typeof magic;
    validator: never[];
};
export default _default;
