import { EineRouteHandlerType } from "../types";
declare function install({ res, server }: EineRouteHandlerType): void;
declare const _default: {
    handler: typeof install;
    validator: never[];
};
export default _default;
